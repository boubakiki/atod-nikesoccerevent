type Data
	@model
	@key(
		name: "datasByDate"
		fields: ["type", "createdAt"]
		queryField: "datasByDate"
	) {
	id: ID!
	type: String!
	name: String!
	firstName: String!
	lastName: String!
	phoneNumber: String!
	position: String
	pScore: String
	rScore: String
	sScore: String
	tScore: String
	createdAt: String!
}
