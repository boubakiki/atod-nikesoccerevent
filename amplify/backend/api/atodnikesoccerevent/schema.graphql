type Data
	@model
	@key(
		name: "datasByDate"
		fields: ["type", "createdAt"]
		queryField: "datasByDate"
	) {
	id: ID!
	type: String!
	order: Int!
	name: String!
	firstName: String!
	lastName: String!
	phoneNumber: String!
	position: String
	pScore: String
	rScore: String
	sScore: String
	tScore: String
	createdAt: String!
}

type Total @model {
	id: ID!
	num: Int!
}